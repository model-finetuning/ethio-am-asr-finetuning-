# NVIDIA GB10 / Linux aarch64: PyTorch 2.13 uses the stable CUDA 13 build.
torch>=2.13.0,<2.14

# TorchAudio 2.11 uses PyTorch's stable ABI and supports PyTorch 2.11+.
torchaudio==2.11.0

# Bundles libsndfile in its Linux ARM64 wheel, avoiding a system FFmpeg install.
soundfile>=0.14.0,<0.15

# ASR training stack
transformers>=4.50.0
accelerate>=1.2.0
datasets>=4.0.0
huggingface_hub>=0.30.0
fsspec>=2024.12.0
jiwer>=3.0.0
numpy>=1.26.0
safetensors>=0.4.5
